Crossword
Автор: Василюс Софія Ярославівна

# Crossword
CROSSWORD/
│
├── .angular/                  # службова папка Angular CLI (автоматично)
├── .vscode/                   # налаштування VSCode
├── node_modules/              # залежності проєкту (автоматично npm install)
│
├── public/
│   └── favicon.ico            # favicon сайту
│
├── src/
│   ├── app/
│   │   ├── components/
│   │   │   ├── editor/        # ✏️ Конструктор кросворду (створення/редагування)
│   │   │   │   ├── editor.component.html
│   │   │   │   ├── editor.component.scss
│   │   │   │   └── editor.component.ts
│   │   │   │
│   │   │   └── player/        # 🎮 Проходження кросворду (режим гравця)
│   │   │       ├── player.component.html
│   │   │       ├── player.component.scss
│   │   │       └── player.component.ts
│   │   │
│   │   ├── models/            # 📦 Моделі даних (структури об'єктів)
│   │   │   └── crossword.ts   # Інтерфейси Crossword, Cell, Entry тощо
│   │   │
│   │   ├── services/          # ⚙️ Сервіси бізнес-логіки
│   │   │   ├── crossword.service.ts  # Генерація, зміна, збереження кросворду
│   │   │   └── storage.service.ts    # Локальне збереження в LocalStorage
│   │   │
│   │   ├── app.config.server.ts  # Конфігурація для SSR (Node-сервер)
│   │   ├── app.config.ts         # Основна конфігурація додатку (standalone bootstrap)
│   │   ├── app.html              # Шаблон головного компонента (root)
│   │   ├── app.routes.server.ts  # Маршрути для серверного рендерингу
│   │   ├── app.routes.ts         # Маршрутизація між Editor / Player
│   │   ├── app.scss              # Глобальні стилі
│   │   ├── app.spec.ts           # Тестовий файл для app.ts
│   │   └── app.ts                # Головний компонент (AppComponent)
│   │
│   ├── index.html                # Кореневий HTML (він завантажує Angular)
│   ├── main.server.ts            # Точка входу для SSR (Angular Universal)
│   ├── main.ts                   # Основна точка входу Angular (браузер)
│   ├── server.ts                 # Сервер Node.js для SSR
│   └── styles.scss               # Глобальні стилі для всього застосунку
│
├── .editorconfig                 # Єдині правила форматування коду
├── .gitignore                    # Ігноровані файли для Git
└── package.json                  # Залежності, скрипти npm, версії бібліотек






🔷 src/app/components/editor/ — Конструктор кросворду
editor.component.html

UI редактора:

поля заголовку, автора

зміна рядків/стовпців

кнопка Reindex

grid-сітка для створення кросворду

блок введення підказок Across / Down

editor.component.scss

Темна тема GitHub-style:

оформлення toolbar

стилізація клітинок .tile, блокованих .block

стиль grid-контейнера

оформлення списку підказок

editor.component.ts

Бізнес-логіка редактора:

обробка введення букв

toggleBlock(cell) — робить клітинку чорною

reindexGrid() — перевизначає grid

виклик CrosswordService

імпорт/експорт JSON

збереження у LocalStorage

🔷 src/app/components/player/ — Режим проходження кросворду
player.component.html

UI для проходження:

сітка кросворду

введення відповіді

підсвітка активної клітинки

відображення підказок

player.component.scss

Стилі гравця:

темна тема

підсвітка активного слова

плавна навігація між клітинками

player.component.ts

Логіка проходження:

введення символів

перехід між клітинками

перевірка правильності

робота зі збереженим JSON

реакція на блоки та межі

🔷 src/app/models/crossword.ts — Моделі даних

Описує структури:

Cell

Entry

Crossword

Це основа всієї програми — типи, з якими працює редактор і гравець.

🔷 src/app/services/ — Сервіси
crossword.service.ts

Керує даними кросворду:

створення сітки (rows × cols)

генерація номерів клітинок

визначення Across / Down entries

оновлення підказок

логіка реіндексації

storage.service.ts

Обгортка над LocalStorage:

зберігає кросворд

завантажує минулий стан

очищає/оновлює дані

🔷 src/app/app.ts — головний компонент

Містить:

перемикачі вкладок “Конструктор / Проходження”

<router-outlet>

базове оформлення сторінки

🔷 src/app/app.html

Шаблон AppComponent.

🔷 src/app/app.routes.ts

Маршрутизація:

/editor → EditorComponent  
/player → PlayerComponent  

🔷 src/app/app.config.ts

Bootstrap Angular:

import RouterModule

підключення глобальних стилів

DI-сервіси

🔷 src/main.ts

Точка запуску Angular у браузері.

🔷 src/main.server.ts

Серверна точка входу для SSR (optional).

🔷 src/server.ts

Node.js SSR сервер для Angular Universal.

🔷 src/index.html

Кореневий HTML:

<app-root>

підключення стилів

мета-теги

🔷 src/styles.scss

Глобальні стилі всього застосунку (фони, шрифти, root-змінні, глобальні reset-стилі).

🚀 Функціонал застосунку
✏️ Конструктор:

створення кросворду будь-якого розміру

редагування клітинок

перетворення клітинки на чорну

авто-нумерація слів

авто-генерація списку Across/Down

імпорт / експорт JSON

збереження в LocalStorage

🎮 Проходження:

повноцінне введення відповідей

автоматичний перехід по літерах

підсвітка активного слова

блоковані клітинки пропускаються

зворотна перевірка по словах

🛠 Технології

Angular 17 (standalone components)

TypeScript

SCSS

LocalStorage

Angular Router

SSR-ready конфігурація



.

📦 CrosswordService — головна логіка роботи кросворду

Цей сервіс відповідає за створення, зміну, нумерацію та перевірку кросворду.
Це “мозок” усього застосунку.

🔹 createEmpty(rows, cols, title, author)

Створює новий порожній кросворд з вказаними розмірами.

генерує порожню сітку Cell[][];

проставляє row, col;

всі клітинки не заблоковані (isBlock: false);

створює об’єкт Crossword;

викликає reindex() → авто-нумерація слів;

повертає готовий Crossword.

🔹 toggleBlock(p, r, c)

Перетворює звичайну клітинку в чорну (блок) або навпаки.

інвертує isBlock;

при блокуванні очищає:

літеру (solution)

номер (number)

посилання на entry (entryId)

після будь-якої зміни виконує:

reindex() → оновлення нумерації Across/Down

touch() → оновлення updatedAt

🔹 setLetter(p, r, c, ch)

Встановлює букву у клітинку.

ігнорує чорні клітинки

допускає тільки літери:
/[A-ZА-ЯІЇЄҐ]/u

автоматично переводить символ у верхній регістр

очищає літеру, якщо ch = null

викликає touch()

🔹 reindex(p)

Найважливіший метод сервісу.
Повністю перераховує слова у кросворді.

Що робить:

Зберігає існуючі підказки (щоб вони не зникли після зміни сітки)

Очищає службові поля клітинок:

number

entryId

Визначає точки початку слова:

isStartAcross(r, c)

isStartDown(r, c)

Для кожного початку слова:

визначає довжину (lineLength())

створює новий Entry:

id = 'A-5' або D-5

номер слова

напрямок

координати

довжину слова

збережену підказку

проставляє entryId усім клітинкам цього слова

Проставляє номера (cell.number = number)

Оновлює p.entries

touch(p)

➡ Цей метод гарантує правильну нумерацію як у New York Times crossword.

🔹 lineLength(p, r, c, dir)

Рахує довжину слова в заданому напрямку.

для across рухається вправо

для down — вниз

зупиняється на чорній клітинці

🔹 setClue(p, entryId, clue)

Оновлює підказку для конкретного слова.

🔹 checkAnswers(p, user)

Перевіряє користувацькі відповіді проти правильних (solution).

Повертає карту помилок:

errors[r][c] = true  // якщо літера неправильна


Це використовується у PlayerComponent для підсвітки.

🔹 export(p)

Повертає JSON у форматі:

{
  "rows": 13,
  "cols": 13,
  "grid": [...],
  "entries": [...],
  "title": "...",
  "author": "...",
  "updatedAt": "..."
}

🔹 import(json)

Імпортує кросворд з JSON.

відновлює структуру

проставляє актуальний updatedAt

запускає reindex()

Тобто можна імпортувати старі кросворди з будь-якої версії.

🔹 touch(p)

Оновлює поле:

p.updatedAt = new Date().toISOString()


Використовується як позначка “останньої зміни”.





🧩 EditorComponent — Конструктор кросворду

Цей компонент відповідає за створення, редагування і збереження кросворду.
Користувач редагує сітку, вставляє літери, ставить блоки, додає підказки.

🔷 Основні залежності
imports: [CommonModule, FormsModule]


✔ дозволяє використовувати ngIf, ngFor, [(ngModel)], двосторонні прив’язки.

constructor(public svc: CrosswordService, private store: StorageService) {}


CrosswordService → логіка кросворду

StorageService → локальне збереження у LocalStorage

🔷 Поля та геттери
@Input() puzzle!: Crossword;

Отримує об'єкт кросворду від AppComponent.

acrossEntries / downEntries

Фільтрують підказки:

get acrossEntries() {
  return this.puzzle?.entries?.filter(e => e.direction === 'across') || [];
}
get downEntries() {
  return this.puzzle?.entries?.filter(e => e.direction === 'down') || [];
}


✔ Завдяки цьому в HTML немає .filter(...)
✔ Оптимізує продуктивність і підвищує читабельність

🔷 Методи редагування
📌 newGrid(rows, cols)

Створює абсолютно новий кросворд з нуля:

this.puzzle = this.svc.createEmpty(rows, cols, title, author);


✔ Повністю замінює попередню сітку
✔ Викликає persist() — зберігає стан

📌 toggleBlock(c: Cell)

Вмикає/вимикає чорну клітинку:

інвертує isBlock

очищає розв’язок і номер

перевикликає reindex()

викликає persist()

Це основний спосіб формування структури кросворду.

📌 typeLetter(c, ev)

Обробляє натискання клавіш у редакторі:

Backspace/Delete → видалити літеру

. або # → перетворити на чорну клітинку

будь-яка літера → встановити букву

завжди викликає persist()

Це робить редактор повноцінним текстовим інтерфейсом.

📌 setClue(id, value)

Оновлює підказку для конкретного слова:

this.svc.setClue(puzzle, id, value);
this.persist();

🔷 Оновлення розмірів сітки
📌 reindexGrid()

Відповідає за перевизначення сітки при зміні Rows/Cols.

Генерує нову сітку:

newGrid = Array.from({ length: rows }).map(...)


Замінює стару

Викликає svc.reindex()

Зберігає зміни

🔷 Імпорт та експорт
📌 export()

Завантажує JSON-файл з кросвордом.

📌 importFile(files)

Зчитує JSON → викликає svc.import() → зберігає стан.

🔷 Збереження
📌 private persist()

Кожна зміна викликає:

this.store.saveEditor(this.puzzle);


Це дозволяє:

оновити сторінку без втрати даних

продовжити роботу з кросвордом у будь-який момент

🎮 PlayerComponent — режим проходження кросворду

Гравець переглядає кросворд, вводить відповіді і може перевірити результат.

🔷 Основні поля
@Input() puzzle!: Crossword

Отримує кросворд, згенерований у редакторі.

dir: Direction

Поточний напрямок руху:

'across' або 'down'

sel = { r, c }

Поточна обрана клітинка.

userGrid: string[][]

Сітка з відповідями користувача.

errors: boolean[][]

Показує, які клітинки неправильні.

completed = signal(false)

React-сигнал Angular — true, коли кросворд повністю розгадано.

🔷 ngOnInit()

Виконує 4 головні речі:

Створює порожній userGrid

Завантажує прогрес із LocalStorage (якщо є)

Створює списки Across/Down

Виконує первинну перевірку (checkAnswers)

🔷 Основні дії користувача
📌 clickCell(r, c)

Обирає клітинку, якщо це не блок.

📌 key(e)

Обробляє всі клавіші:

стрілки → рух

Backspace/Delete → стирання літери

літера → вводиться у клітинку

Tab → перейти до наступного слова

визначає правильні символи, пропускає блоки

📌 toggleDir()

Змінює напрямок набору Across ⇆ Down.

📌 put(ch, r, c)

Вставляє літеру:

записує у userGrid

перевіряє відповідь

оновлює прогрес

зберігає стан

📌 step(delta)

Переміщається в наступну/попередню клітинку у межах слова.

📌 move(dx, dy)

Класичні стрілки — рух по всій сітці.

📌 jumpNextEntry(dir)

Перехід до наступного слова:

знаходить entryId всієї сітки

обчислює наступне слово

перемикає напрямок і позицію

📌 Допоміжні функції
check()

Перевіряє всі літери на правильність.

revealCell()

Показує правильну літеру у поточній клітинці.

revealWord()

Автоматично розкриває ціле слово.

reset()

Очищає весь введений прогрес гравця.

🔷 Система завершення
updateCompletion()

Перевіряє, чи:

кожна клітинка має розв’язок

кожна введена літера відповідає правильній (solution)

Якщо так → completed.set(true).

🧩 Взаємодія Editor ↔ Player

EditorComponent створює і редагує Crossword

CrosswordService зберігає структуру

StorageService кешує дані

PlayerComponent працює з тим самим Crossword

Таким чином застосунок — це повноцінна система:

✔ конструктор → ✔ збереження → ✔ проходження → ✔ перевірка